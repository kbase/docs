----------TEST meme compare_motifs_with_tomtom_job_by_collection_from_ws with -h option
	okay

NAME
compare_motifs_with_tomtom_job_by_collection_from_ws - compare two collections of sequence motifs by TOMTOM.


VERSION
1.0

SYNOPSIS
compare_motifs_with_tomtom_job_by_collection_from_ws [--url=http://140.221.85.173:7077/ --ws=<workspace ID> --query=<MemePSPMCollection reference> --target=<MemePSPMCollection reference> --matrix=<PSPM ID> --thresh=<threshold> --evalue --dist=<allr|ed|kullback|pearson|sandelin> --internal --min_overlap=<value> --user=<username> --pw=<password>] 

DESCRIPTION
INPUT:            This command requires the URL of the service, references of two PSPM collections and parameters.

OUTPUT:           The output of this command is a Job object ID.

PARAMETERS:
--url             The URL of the service, --url=http://140.221.85.173:7077/, required.

--ws              Name of workspace where run result will be stored, required.

--query           Workspace reference of the query PSPM collection, required.

--target           Workspace reference of the target PSPM collection, required.

--matrix           KBase ID of a MemePSPM from the query collection that will be used. If omitted, all motifs in the query collection will be used.

--thresh          Only report matches with significance values ≤ thresh parameter value. 

--evalue          Use the E-value of the match as the significance threshold.

--dist            Motif distance calculation algorithm (accepable values are "allr", "ed", "kullback", "pearson", "sandelin").

--internal        This parameter forces the shorter motif to be completely contained in the longer motif.

--min_overlap     Only report motif matches that overlap by min overlap positions or more.

--user            User name for access to workspace.

--pw              Password for access to workspace.

--help            Display help message to standard out and exit with error code zero; 
                  ignore all other command-line arguments.  
--version         Print version information. 

 
EXAMPLES 
compare_motifs_with_tomtom_job_by_collection_from_ws --url=http://140.221.85.173:7077/ --ws=AKtest --query="AKtest/kb|memepspmcollection.2" --target="AKtest/kb|memepspmcollection.2" --thresh=0.0000001 --evalue --dist=pearson --internal --min_overlap=12

This command will return a Job object ID.

 
Report bugs to aekazakov@lbl.gov
NAME
compare_motifs_with_tomtom_job_by_collection_from_ws - compare two collections of sequence motifs by TOMTOM.


VERSION
1.0

SYNOPSIS
compare_motifs_with_tomtom_job_by_collection_from_ws [--url=http://140.221.85.173:7077/ --ws=<workspace ID> --query=<MemePSPMCollection reference> --target=<MemePSPMCollection reference> --matrix=<PSPM ID> --thresh=<threshold> --evalue --dist=<allr|ed|kullback|pearson|sandelin> --internal --min_overlap=<value> --user=<username> --pw=<password>] 

DESCRIPTION
INPUT:            This command requires the URL of the service, references of two PSPM collections and parameters.

OUTPUT:           The output of this command is a Job object ID.

PARAMETERS:
--url             The URL of the service, --url=http://140.221.85.173:7077/, required.

--ws              Name of workspace where run result will be stored, required.

--query           Workspace reference of the query PSPM collection, required.

--target           Workspace reference of the target PSPM collection, required.

--matrix           KBase ID of a MemePSPM from the query collection that will be used. If omitted, all motifs in the query collection will be used.

--thresh          Only report matches with significance values ≤ thresh parameter value. 

--evalue          Use the E-value of the match as the significance threshold.

--dist            Motif distance calculation algorithm (accepable values are "allr", "ed", "kullback", "pearson", "sandelin").

--internal        This parameter forces the shorter motif to be completely contained in the longer motif.

--min_overlap     Only report motif matches that overlap by min overlap positions or more.

--user            User name for access to workspace.

--pw              Password for access to workspace.

--help            Display help message to standard out and exit with error code zero; 
                  ignore all other command-line arguments.  
--version         Print version information. 

 
EXAMPLES 
compare_motifs_with_tomtom_job_by_collection_from_ws --url=http://140.221.85.173:7077/ --ws=AKtest --query="AKtest/kb|memepspmcollection.2" --target="AKtest/kb|memepspmcollection.2" --thresh=0.0000001 --evalue --dist=pearson --internal --min_overlap=12

This command will return a Job object ID.

 
Report bugs to aekazakov@lbl.gov
----------TEST meme compare_motifs_with_tomtom_job_by_collection_from_ws with --help option
	okay

NAME
compare_motifs_with_tomtom_job_by_collection_from_ws - compare two collections of sequence motifs by TOMTOM.


VERSION
1.0

SYNOPSIS
compare_motifs_with_tomtom_job_by_collection_from_ws [--url=http://140.221.85.173:7077/ --ws=<workspace ID> --query=<MemePSPMCollection reference> --target=<MemePSPMCollection reference> --matrix=<PSPM ID> --thresh=<threshold> --evalue --dist=<allr|ed|kullback|pearson|sandelin> --internal --min_overlap=<value> --user=<username> --pw=<password>] 

DESCRIPTION
INPUT:            This command requires the URL of the service, references of two PSPM collections and parameters.

OUTPUT:           The output of this command is a Job object ID.

PARAMETERS:
--url             The URL of the service, --url=http://140.221.85.173:7077/, required.

--ws              Name of workspace where run result will be stored, required.

--query           Workspace reference of the query PSPM collection, required.

--target           Workspace reference of the target PSPM collection, required.

--matrix           KBase ID of a MemePSPM from the query collection that will be used. If omitted, all motifs in the query collection will be used.

--thresh          Only report matches with significance values ≤ thresh parameter value. 

--evalue          Use the E-value of the match as the significance threshold.

--dist            Motif distance calculation algorithm (accepable values are "allr", "ed", "kullback", "pearson", "sandelin").

--internal        This parameter forces the shorter motif to be completely contained in the longer motif.

--min_overlap     Only report motif matches that overlap by min overlap positions or more.

--user            User name for access to workspace.

--pw              Password for access to workspace.

--help            Display help message to standard out and exit with error code zero; 
                  ignore all other command-line arguments.  
--version         Print version information. 

 
EXAMPLES 
compare_motifs_with_tomtom_job_by_collection_from_ws --url=http://140.221.85.173:7077/ --ws=AKtest --query="AKtest/kb|memepspmcollection.2" --target="AKtest/kb|memepspmcollection.2" --thresh=0.0000001 --evalue --dist=pearson --internal --min_overlap=12

This command will return a Job object ID.

 
Report bugs to aekazakov@lbl.gov
NAME
compare_motifs_with_tomtom_job_by_collection_from_ws - compare two collections of sequence motifs by TOMTOM.


VERSION
1.0

SYNOPSIS
compare_motifs_with_tomtom_job_by_collection_from_ws [--url=http://140.221.85.173:7077/ --ws=<workspace ID> --query=<MemePSPMCollection reference> --target=<MemePSPMCollection reference> --matrix=<PSPM ID> --thresh=<threshold> --evalue --dist=<allr|ed|kullback|pearson|sandelin> --internal --min_overlap=<value> --user=<username> --pw=<password>] 

DESCRIPTION
INPUT:            This command requires the URL of the service, references of two PSPM collections and parameters.

OUTPUT:           The output of this command is a Job object ID.

PARAMETERS:
--url             The URL of the service, --url=http://140.221.85.173:7077/, required.

--ws              Name of workspace where run result will be stored, required.

--query           Workspace reference of the query PSPM collection, required.

--target           Workspace reference of the target PSPM collection, required.

--matrix           KBase ID of a MemePSPM from the query collection that will be used. If omitted, all motifs in the query collection will be used.

--thresh          Only report matches with significance values ≤ thresh parameter value. 

--evalue          Use the E-value of the match as the significance threshold.

--dist            Motif distance calculation algorithm (accepable values are "allr", "ed", "kullback", "pearson", "sandelin").

--internal        This parameter forces the shorter motif to be completely contained in the longer motif.

--min_overlap     Only report motif matches that overlap by min overlap positions or more.

--user            User name for access to workspace.

--pw              Password for access to workspace.

--help            Display help message to standard out and exit with error code zero; 
                  ignore all other command-line arguments.  
--version         Print version information. 

 
EXAMPLES 
compare_motifs_with_tomtom_job_by_collection_from_ws --url=http://140.221.85.173:7077/ --ws=AKtest --query="AKtest/kb|memepspmcollection.2" --target="AKtest/kb|memepspmcollection.2" --thresh=0.0000001 --evalue --dist=pearson --internal --min_overlap=12

This command will return a Job object ID.

 
Report bugs to aekazakov@lbl.gov
----------TEST meme get_pspm_collection_from_meme_result_job_from_ws with -h option
	okay

NAME
get_pspm_collection_from_meme_result_job_from_ws - converts MEME run result into collection of position-specific probability matrices


VERSION
1.0

SYNOPSIS
get_pspm_collection_from_meme_result_job_from_ws [--url=http://140.221.85.173:7077/ --ws=<workspace ID> --input=<MemeRunResult ID> --user=<username> --pw=<password>]

DESCRIPTION
INPUT:            This command requires the URL of the service, workspace name, workspace reference of MemeRunresult, .

OUTPUT:           The output of this command is Job object ID.

PARAMETERS:
--url             The URL of the service, --url=http://140.221.85.173:7077/, required.

--ws              Workspace name, required.

--input           Workspace reference of the MemeRunResult , required.

--user            User name for access to workspace.

--pw              Password for access to workspace.

--help            Display help message to standard out and exit with error code zero; 
                  ignore all other command-line arguments.  
--version         Print version information. 

 
EXAMPLES 
get_pspm_collection_from_meme_result_job_from_ws --url=http://140.221.85.173:7077/ --ws=AKtest --input="AKtest/kb|memerunresult.15"

This command will return Job object ID.
 
Report bugs to aekazakov@lbl.gov
NAME
get_pspm_collection_from_meme_result_job_from_ws - converts MEME run result into collection of position-specific probability matrices


VERSION
1.0

SYNOPSIS
get_pspm_collection_from_meme_result_job_from_ws [--url=http://140.221.85.173:7077/ --ws=<workspace ID> --input=<MemeRunResult ID> --user=<username> --pw=<password>]

DESCRIPTION
INPUT:            This command requires the URL of the service, workspace name, workspace reference of MemeRunresult, .

OUTPUT:           The output of this command is Job object ID.

PARAMETERS:
--url             The URL of the service, --url=http://140.221.85.173:7077/, required.

--ws              Workspace name, required.

--input           Workspace reference of the MemeRunResult , required.

--user            User name for access to workspace.

--pw              Password for access to workspace.

--help            Display help message to standard out and exit with error code zero; 
                  ignore all other command-line arguments.  
--version         Print version information. 

 
EXAMPLES 
get_pspm_collection_from_meme_result_job_from_ws --url=http://140.221.85.173:7077/ --ws=AKtest --input="AKtest/kb|memerunresult.15"

This command will return Job object ID.
 
Report bugs to aekazakov@lbl.gov
----------TEST meme get_pspm_collection_from_meme_result_job_from_ws with --help option
	okay

NAME
get_pspm_collection_from_meme_result_job_from_ws - converts MEME run result into collection of position-specific probability matrices


VERSION
1.0

SYNOPSIS
get_pspm_collection_from_meme_result_job_from_ws [--url=http://140.221.85.173:7077/ --ws=<workspace ID> --input=<MemeRunResult ID> --user=<username> --pw=<password>]

DESCRIPTION
INPUT:            This command requires the URL of the service, workspace name, workspace reference of MemeRunresult, .

OUTPUT:           The output of this command is Job object ID.

PARAMETERS:
--url             The URL of the service, --url=http://140.221.85.173:7077/, required.

--ws              Workspace name, required.

--input           Workspace reference of the MemeRunResult , required.

--user            User name for access to workspace.

--pw              Password for access to workspace.

--help            Display help message to standard out and exit with error code zero; 
                  ignore all other command-line arguments.  
--version         Print version information. 

 
EXAMPLES 
get_pspm_collection_from_meme_result_job_from_ws --url=http://140.221.85.173:7077/ --ws=AKtest --input="AKtest/kb|memerunresult.15"

This command will return Job object ID.
 
Report bugs to aekazakov@lbl.gov
NAME
get_pspm_collection_from_meme_result_job_from_ws - converts MEME run result into collection of position-specific probability matrices


VERSION
1.0

SYNOPSIS
get_pspm_collection_from_meme_result_job_from_ws [--url=http://140.221.85.173:7077/ --ws=<workspace ID> --input=<MemeRunResult ID> --user=<username> --pw=<password>]

DESCRIPTION
INPUT:            This command requires the URL of the service, workspace name, workspace reference of MemeRunresult, .

OUTPUT:           The output of this command is Job object ID.

PARAMETERS:
--url             The URL of the service, --url=http://140.221.85.173:7077/, required.

--ws              Workspace name, required.

--input           Workspace reference of the MemeRunResult , required.

--user            User name for access to workspace.

--pw              Password for access to workspace.

--help            Display help message to standard out and exit with error code zero; 
                  ignore all other command-line arguments.  
--version         Print version information. 

 
EXAMPLES 
get_pspm_collection_from_meme_result_job_from_ws --url=http://140.221.85.173:7077/ --ws=AKtest --input="AKtest/kb|memerunresult.15"

This command will return Job object ID.
 
Report bugs to aekazakov@lbl.gov
----------TEST meme compare_motifs_with_tomtom_by_collection_from_ws with -h option
	okay

NAME
compare_motifs_with_tomtom_by_collection_from_ws - compare two collections of sequence motifs by TOMTOM.


VERSION
1.0

SYNOPSIS
compare_motifs_with_tomtom_by_collection_from_ws [--url=http://140.221.85.173:7077/ --ws=<workspace ID> --query=<MemePSPMCollection reference> --target=<MemePSPMCollection reference> --matrix=<PSPM ID> --thresh=<threshold> --evalue --dist=<allr|ed|kullback|pearson|sandelin> --internal --min_overlap=<value> --user=<username> --pw=<password>] 

DESCRIPTION
INPUT:            This command requires the URL of the service, IDs of two PSPM collections and parameters.

OUTPUT:           The output of this command is an ID of TomtomRunResult.

PARAMETERS:
--url             The URL of the service, --url=http://140.221.85.173:7077/, required.

--ws              Name of workspace where run result will be stored, required.

--query           Workspace reference of the query PSPM collection, required.

--target           Workspace reference of the target PSPM collection, required.

--matrix           KBase ID of a MemePSPM from the query collection that will be used. If omitted, all motifs in the query collection will be used.

--thresh          Only report matches with significance values ≤ thresh parameter value. 

--evalue          Use the E-value of the match as the significance threshold.

--dist            Motif distance calculation algorithm (accepable values are "allr", "ed", "kullback", "pearson", "sandelin").

--internal        This parameter forces the shorter motif to be completely contained in the longer motif.

--min_overlap     Only report motif matches that overlap by min overlap positions or more.

--user            User name for access to workspace.

--pw              Password for access to workspace.

--help            Display help message to standard out and exit with error code zero; 
                  ignore all other command-line arguments.  
--version         Print version information. 

 
EXAMPLES 
compare_motifs_with_tomtom_by_collection_from_ws --url=http://140.221.85.173:7077/ --ws=AKtest --query="AKtest/kb|memepspmcollection.1" --target="AKtest/kb|memepspmcollection.1" --thresh=0.0000001 --evalue --dist=pearson --internal --min_overlap=12

This command will return an ID of list of TOMTOM hits.

 
Report bugs to aekazakov@lbl.gov
NAME
compare_motifs_with_tomtom_by_collection_from_ws - compare two collections of sequence motifs by TOMTOM.


VERSION
1.0

SYNOPSIS
compare_motifs_with_tomtom_by_collection_from_ws [--url=http://140.221.85.173:7077/ --ws=<workspace ID> --query=<MemePSPMCollection reference> --target=<MemePSPMCollection reference> --matrix=<PSPM ID> --thresh=<threshold> --evalue --dist=<allr|ed|kullback|pearson|sandelin> --internal --min_overlap=<value> --user=<username> --pw=<password>] 

DESCRIPTION
INPUT:            This command requires the URL of the service, IDs of two PSPM collections and parameters.

OUTPUT:           The output of this command is an ID of TomtomRunResult.

PARAMETERS:
--url             The URL of the service, --url=http://140.221.85.173:7077/, required.

--ws              Name of workspace where run result will be stored, required.

--query           Workspace reference of the query PSPM collection, required.

--target           Workspace reference of the target PSPM collection, required.

--matrix           KBase ID of a MemePSPM from the query collection that will be used. If omitted, all motifs in the query collection will be used.

--thresh          Only report matches with significance values ≤ thresh parameter value. 

--evalue          Use the E-value of the match as the significance threshold.

--dist            Motif distance calculation algorithm (accepable values are "allr", "ed", "kullback", "pearson", "sandelin").

--internal        This parameter forces the shorter motif to be completely contained in the longer motif.

--min_overlap     Only report motif matches that overlap by min overlap positions or more.

--user            User name for access to workspace.

--pw              Password for access to workspace.

--help            Display help message to standard out and exit with error code zero; 
                  ignore all other command-line arguments.  
--version         Print version information. 

 
EXAMPLES 
compare_motifs_with_tomtom_by_collection_from_ws --url=http://140.221.85.173:7077/ --ws=AKtest --query="AKtest/kb|memepspmcollection.1" --target="AKtest/kb|memepspmcollection.1" --thresh=0.0000001 --evalue --dist=pearson --internal --min_overlap=12

This command will return an ID of list of TOMTOM hits.

 
Report bugs to aekazakov@lbl.gov
----------TEST meme compare_motifs_with_tomtom_by_collection_from_ws with --help option
	okay

NAME
compare_motifs_with_tomtom_by_collection_from_ws - compare two collections of sequence motifs by TOMTOM.


VERSION
1.0

SYNOPSIS
compare_motifs_with_tomtom_by_collection_from_ws [--url=http://140.221.85.173:7077/ --ws=<workspace ID> --query=<MemePSPMCollection reference> --target=<MemePSPMCollection reference> --matrix=<PSPM ID> --thresh=<threshold> --evalue --dist=<allr|ed|kullback|pearson|sandelin> --internal --min_overlap=<value> --user=<username> --pw=<password>] 

DESCRIPTION
INPUT:            This command requires the URL of the service, IDs of two PSPM collections and parameters.

OUTPUT:           The output of this command is an ID of TomtomRunResult.

PARAMETERS:
--url             The URL of the service, --url=http://140.221.85.173:7077/, required.

--ws              Name of workspace where run result will be stored, required.

--query           Workspace reference of the query PSPM collection, required.

--target           Workspace reference of the target PSPM collection, required.

--matrix           KBase ID of a MemePSPM from the query collection that will be used. If omitted, all motifs in the query collection will be used.

--thresh          Only report matches with significance values ≤ thresh parameter value. 

--evalue          Use the E-value of the match as the significance threshold.

--dist            Motif distance calculation algorithm (accepable values are "allr", "ed", "kullback", "pearson", "sandelin").

--internal        This parameter forces the shorter motif to be completely contained in the longer motif.

--min_overlap     Only report motif matches that overlap by min overlap positions or more.

--user            User name for access to workspace.

--pw              Password for access to workspace.

--help            Display help message to standard out and exit with error code zero; 
                  ignore all other command-line arguments.  
--version         Print version information. 

 
EXAMPLES 
compare_motifs_with_tomtom_by_collection_from_ws --url=http://140.221.85.173:7077/ --ws=AKtest --query="AKtest/kb|memepspmcollection.1" --target="AKtest/kb|memepspmcollection.1" --thresh=0.0000001 --evalue --dist=pearson --internal --min_overlap=12

This command will return an ID of list of TOMTOM hits.

 
Report bugs to aekazakov@lbl.gov
NAME
compare_motifs_with_tomtom_by_collection_from_ws - compare two collections of sequence motifs by TOMTOM.


VERSION
1.0

SYNOPSIS
compare_motifs_with_tomtom_by_collection_from_ws [--url=http://140.221.85.173:7077/ --ws=<workspace ID> --query=<MemePSPMCollection reference> --target=<MemePSPMCollection reference> --matrix=<PSPM ID> --thresh=<threshold> --evalue --dist=<allr|ed|kullback|pearson|sandelin> --internal --min_overlap=<value> --user=<username> --pw=<password>] 

DESCRIPTION
INPUT:            This command requires the URL of the service, IDs of two PSPM collections and parameters.

OUTPUT:           The output of this command is an ID of TomtomRunResult.

PARAMETERS:
--url             The URL of the service, --url=http://140.221.85.173:7077/, required.

--ws              Name of workspace where run result will be stored, required.

--query           Workspace reference of the query PSPM collection, required.

--target           Workspace reference of the target PSPM collection, required.

--matrix           KBase ID of a MemePSPM from the query collection that will be used. If omitted, all motifs in the query collection will be used.

--thresh          Only report matches with significance values ≤ thresh parameter value. 

--evalue          Use the E-value of the match as the significance threshold.

--dist            Motif distance calculation algorithm (accepable values are "allr", "ed", "kullback", "pearson", "sandelin").

--internal        This parameter forces the shorter motif to be completely contained in the longer motif.

--min_overlap     Only report motif matches that overlap by min overlap positions or more.

--user            User name for access to workspace.

--pw              Password for access to workspace.

--help            Display help message to standard out and exit with error code zero; 
                  ignore all other command-line arguments.  
--version         Print version information. 

 
EXAMPLES 
compare_motifs_with_tomtom_by_collection_from_ws --url=http://140.221.85.173:7077/ --ws=AKtest --query="AKtest/kb|memepspmcollection.1" --target="AKtest/kb|memepspmcollection.1" --thresh=0.0000001 --evalue --dist=pearson --internal --min_overlap=12

This command will return an ID of list of TOMTOM hits.

 
Report bugs to aekazakov@lbl.gov
----------TEST meme find_motifs_with_meme_from_ws with -h option
	okay

NAME
find_motifs_with_meme_from_ws - This command reconstructs motifs with MEME in a set of sequences stored in workspace.


VERSION
1.0

SYNOPSIS
find_motifs_with_meme_from_ws [--url=http://140.221.85.173:7077/ --ws=<workspace name> --input=<sequence set reference> --mod=<oops|zoops|anr> --nmotifs=<nmotifs> --minw=<minw> --maxw=<maxw> --nsites=<nsites> --minsites=<minsites> --maxsites=<maxsites> --pal --revcomp --user=<username> --pw=<password>]

DESCRIPTION
INPUT:            This command requires the URL of the service, reference of a sequence set and parameters.

OUTPUT:           This command returns name of MEME run result.

PARAMETERS:
--url             The URL of the service, --url=http://140.221.85.173:7077/, required.

--ws              Workspace name, required.

--input           Workspace reference of the input sequence set, required.

--mod             Distribution of motifs (oops =  One Occurrence Per Sequence, zoops = Zero or One Occurrence Per Sequence, anr = Any Number of Repetitions), required.

--nmotifs         Maximum number of motifs to find, required.

--minw            Minumum motif width, required.

--maxw            Maximum motif width, required.

--nsites          Number of sites for each motif.

--minsites        Minimum number of sites for each motif.

--maxsites        Maximum number of sites for each motif.

--pal             Force palindromes.

--revcomp         Allow sites on + or - DNA strands. Default: look for DNA motifs only on the strand given in the training set.

--user            User name for access to workspace.

--pw              Password for access to workspace.

--help            Display help message to standard out and exit with error code zero; 
                  ignore all other command-line arguments.  
--version         Print version information. 

 
EXAMPLES 
find_motifs_with_meme_from_ws --url=http://140.221.85.173:7077/ --ws=AKtest --input="AKtest/kb|sequenceset.8" --mod=oops --nmotifs=2 --minw=14 --maxw=28 

This command will return a collection of two motifs with length between 14 and 28 bp.


Report bugs to aekazakov@lbl.gov
NAME
find_motifs_with_meme_from_ws - This command reconstructs motifs with MEME in a set of sequences stored in workspace.


VERSION
1.0

SYNOPSIS
find_motifs_with_meme_from_ws [--url=http://140.221.85.173:7077/ --ws=<workspace name> --input=<sequence set reference> --mod=<oops|zoops|anr> --nmotifs=<nmotifs> --minw=<minw> --maxw=<maxw> --nsites=<nsites> --minsites=<minsites> --maxsites=<maxsites> --pal --revcomp --user=<username> --pw=<password>]

DESCRIPTION
INPUT:            This command requires the URL of the service, reference of a sequence set and parameters.

OUTPUT:           This command returns name of MEME run result.

PARAMETERS:
--url             The URL of the service, --url=http://140.221.85.173:7077/, required.

--ws              Workspace name, required.

--input           Workspace reference of the input sequence set, required.

--mod             Distribution of motifs (oops =  One Occurrence Per Sequence, zoops = Zero or One Occurrence Per Sequence, anr = Any Number of Repetitions), required.

--nmotifs         Maximum number of motifs to find, required.

--minw            Minumum motif width, required.

--maxw            Maximum motif width, required.

--nsites          Number of sites for each motif.

--minsites        Minimum number of sites for each motif.

--maxsites        Maximum number of sites for each motif.

--pal             Force palindromes.

--revcomp         Allow sites on + or - DNA strands. Default: look for DNA motifs only on the strand given in the training set.

--user            User name for access to workspace.

--pw              Password for access to workspace.

--help            Display help message to standard out and exit with error code zero; 
                  ignore all other command-line arguments.  
--version         Print version information. 

 
EXAMPLES 
find_motifs_with_meme_from_ws --url=http://140.221.85.173:7077/ --ws=AKtest --input="AKtest/kb|sequenceset.8" --mod=oops --nmotifs=2 --minw=14 --maxw=28 

This command will return a collection of two motifs with length between 14 and 28 bp.


Report bugs to aekazakov@lbl.gov
----------TEST meme find_motifs_with_meme_from_ws with --help option
	okay

NAME
find_motifs_with_meme_from_ws - This command reconstructs motifs with MEME in a set of sequences stored in workspace.


VERSION
1.0

SYNOPSIS
find_motifs_with_meme_from_ws [--url=http://140.221.85.173:7077/ --ws=<workspace name> --input=<sequence set reference> --mod=<oops|zoops|anr> --nmotifs=<nmotifs> --minw=<minw> --maxw=<maxw> --nsites=<nsites> --minsites=<minsites> --maxsites=<maxsites> --pal --revcomp --user=<username> --pw=<password>]

DESCRIPTION
INPUT:            This command requires the URL of the service, reference of a sequence set and parameters.

OUTPUT:           This command returns name of MEME run result.

PARAMETERS:
--url             The URL of the service, --url=http://140.221.85.173:7077/, required.

--ws              Workspace name, required.

--input           Workspace reference of the input sequence set, required.

--mod             Distribution of motifs (oops =  One Occurrence Per Sequence, zoops = Zero or One Occurrence Per Sequence, anr = Any Number of Repetitions), required.

--nmotifs         Maximum number of motifs to find, required.

--minw            Minumum motif width, required.

--maxw            Maximum motif width, required.

--nsites          Number of sites for each motif.

--minsites        Minimum number of sites for each motif.

--maxsites        Maximum number of sites for each motif.

--pal             Force palindromes.

--revcomp         Allow sites on + or - DNA strands. Default: look for DNA motifs only on the strand given in the training set.

--user            User name for access to workspace.

--pw              Password for access to workspace.

--help            Display help message to standard out and exit with error code zero; 
                  ignore all other command-line arguments.  
--version         Print version information. 

 
EXAMPLES 
find_motifs_with_meme_from_ws --url=http://140.221.85.173:7077/ --ws=AKtest --input="AKtest/kb|sequenceset.8" --mod=oops --nmotifs=2 --minw=14 --maxw=28 

This command will return a collection of two motifs with length between 14 and 28 bp.


Report bugs to aekazakov@lbl.gov
NAME
find_motifs_with_meme_from_ws - This command reconstructs motifs with MEME in a set of sequences stored in workspace.


VERSION
1.0

SYNOPSIS
find_motifs_with_meme_from_ws [--url=http://140.221.85.173:7077/ --ws=<workspace name> --input=<sequence set reference> --mod=<oops|zoops|anr> --nmotifs=<nmotifs> --minw=<minw> --maxw=<maxw> --nsites=<nsites> --minsites=<minsites> --maxsites=<maxsites> --pal --revcomp --user=<username> --pw=<password>]

DESCRIPTION
INPUT:            This command requires the URL of the service, reference of a sequence set and parameters.

OUTPUT:           This command returns name of MEME run result.

PARAMETERS:
--url             The URL of the service, --url=http://140.221.85.173:7077/, required.

--ws              Workspace name, required.

--input           Workspace reference of the input sequence set, required.

--mod             Distribution of motifs (oops =  One Occurrence Per Sequence, zoops = Zero or One Occurrence Per Sequence, anr = Any Number of Repetitions), required.

--nmotifs         Maximum number of motifs to find, required.

--minw            Minumum motif width, required.

--maxw            Maximum motif width, required.

--nsites          Number of sites for each motif.

--minsites        Minimum number of sites for each motif.

--maxsites        Maximum number of sites for each motif.

--pal             Force palindromes.

--revcomp         Allow sites on + or - DNA strands. Default: look for DNA motifs only on the strand given in the training set.

--user            User name for access to workspace.

--pw              Password for access to workspace.

--help            Display help message to standard out and exit with error code zero; 
                  ignore all other command-line arguments.  
--version         Print version information. 

 
EXAMPLES 
find_motifs_with_meme_from_ws --url=http://140.221.85.173:7077/ --ws=AKtest --input="AKtest/kb|sequenceset.8" --mod=oops --nmotifs=2 --minw=14 --maxw=28 

This command will return a collection of two motifs with length between 14 and 28 bp.


Report bugs to aekazakov@lbl.gov
----------TEST meme find_motifs_with_meme_job_from_ws with -h option
	okay

NAME
find_motifs_with_meme_job_from_ws - This command reconstructs motifs with MEME in a set of sequences stored in workspace.


VERSION
1.0

SYNOPSIS
find_motifs_with_meme_job_from_ws [--url=http://140.221.85.173:7077/ --ws=<workspace name> --input=<sequence set reference> --mod=<oops|zoops|anr> --nmotifs=<nmotifs> --minw=<minw> --maxw=<maxw> --nsites=<nsites> --minsites=<minsites> --maxsites=<maxsites> --pal --revcomp --user=<username> --pw=<password>]

DESCRIPTION
INPUT:            This command requires the URL of the service, ID of a sequence set and parameters.

OUTPUT:           This command returns Job object ID.

PARAMETERS:
--url             The URL of the service, --url=http://140.221.85.173:7077/, required.

--ws              Workspace name, required.

--input           KBase ID of the input sequence set, required.

--mod             Distribution of motifs (oops =  One Occurrence Per Sequence, zoops = Zero or One Occurrence Per Sequence, anr = Any Number of Repetitions), required.

--nmotifs         Maximum number of motifs to find, required.

--minw            Minumum motif width, required.

--maxw            Maximum motif width, required.

--nsites          Number of sites for each motif.

--minsites        Minimum number of sites for each motif.

--maxsites        Maximum number of sites for each motif.

--pal             Force palindromes.

--revcomp         Allow sites on + or - DNA strands. Default: look for DNA motifs only on the strand given in the training set.

--user            User name for access to workspace.

--pw              Password for access to workspace.

--help            Display help message to standard out and exit with error code zero; 
                  ignore all other command-line arguments.  
--version         Print version information. 

 
EXAMPLES 
find_motifs_with_meme_job_from_ws --url=http://140.221.85.173:7077/ --ws=AKtest --input="AKtest/kb|sequenceset.8" --mod=oops --nmotifs=2 --minw=14 --maxw=28 

This command will return a Job object ID.


Report bugs to aekazakov@lbl.gov
NAME
find_motifs_with_meme_job_from_ws - This command reconstructs motifs with MEME in a set of sequences stored in workspace.


VERSION
1.0

SYNOPSIS
find_motifs_with_meme_job_from_ws [--url=http://140.221.85.173:7077/ --ws=<workspace name> --input=<sequence set reference> --mod=<oops|zoops|anr> --nmotifs=<nmotifs> --minw=<minw> --maxw=<maxw> --nsites=<nsites> --minsites=<minsites> --maxsites=<maxsites> --pal --revcomp --user=<username> --pw=<password>]

DESCRIPTION
INPUT:            This command requires the URL of the service, ID of a sequence set and parameters.

OUTPUT:           This command returns Job object ID.

PARAMETERS:
--url             The URL of the service, --url=http://140.221.85.173:7077/, required.

--ws              Workspace name, required.

--input           KBase ID of the input sequence set, required.

--mod             Distribution of motifs (oops =  One Occurrence Per Sequence, zoops = Zero or One Occurrence Per Sequence, anr = Any Number of Repetitions), required.

--nmotifs         Maximum number of motifs to find, required.

--minw            Minumum motif width, required.

--maxw            Maximum motif width, required.

--nsites          Number of sites for each motif.

--minsites        Minimum number of sites for each motif.

--maxsites        Maximum number of sites for each motif.

--pal             Force palindromes.

--revcomp         Allow sites on + or - DNA strands. Default: look for DNA motifs only on the strand given in the training set.

--user            User name for access to workspace.

--pw              Password for access to workspace.

--help            Display help message to standard out and exit with error code zero; 
                  ignore all other command-line arguments.  
--version         Print version information. 

 
EXAMPLES 
find_motifs_with_meme_job_from_ws --url=http://140.221.85.173:7077/ --ws=AKtest --input="AKtest/kb|sequenceset.8" --mod=oops --nmotifs=2 --minw=14 --maxw=28 

This command will return a Job object ID.


Report bugs to aekazakov@lbl.gov
----------TEST meme find_motifs_with_meme_job_from_ws with --help option
	okay

NAME
find_motifs_with_meme_job_from_ws - This command reconstructs motifs with MEME in a set of sequences stored in workspace.


VERSION
1.0

SYNOPSIS
find_motifs_with_meme_job_from_ws [--url=http://140.221.85.173:7077/ --ws=<workspace name> --input=<sequence set reference> --mod=<oops|zoops|anr> --nmotifs=<nmotifs> --minw=<minw> --maxw=<maxw> --nsites=<nsites> --minsites=<minsites> --maxsites=<maxsites> --pal --revcomp --user=<username> --pw=<password>]

DESCRIPTION
INPUT:            This command requires the URL of the service, ID of a sequence set and parameters.

OUTPUT:           This command returns Job object ID.

PARAMETERS:
--url             The URL of the service, --url=http://140.221.85.173:7077/, required.

--ws              Workspace name, required.

--input           KBase ID of the input sequence set, required.

--mod             Distribution of motifs (oops =  One Occurrence Per Sequence, zoops = Zero or One Occurrence Per Sequence, anr = Any Number of Repetitions), required.

--nmotifs         Maximum number of motifs to find, required.

--minw            Minumum motif width, required.

--maxw            Maximum motif width, required.

--nsites          Number of sites for each motif.

--minsites        Minimum number of sites for each motif.

--maxsites        Maximum number of sites for each motif.

--pal             Force palindromes.

--revcomp         Allow sites on + or - DNA strands. Default: look for DNA motifs only on the strand given in the training set.

--user            User name for access to workspace.

--pw              Password for access to workspace.

--help            Display help message to standard out and exit with error code zero; 
                  ignore all other command-line arguments.  
--version         Print version information. 

 
EXAMPLES 
find_motifs_with_meme_job_from_ws --url=http://140.221.85.173:7077/ --ws=AKtest --input="AKtest/kb|sequenceset.8" --mod=oops --nmotifs=2 --minw=14 --maxw=28 

This command will return a Job object ID.


Report bugs to aekazakov@lbl.gov
NAME
find_motifs_with_meme_job_from_ws - This command reconstructs motifs with MEME in a set of sequences stored in workspace.


VERSION
1.0

SYNOPSIS
find_motifs_with_meme_job_from_ws [--url=http://140.221.85.173:7077/ --ws=<workspace name> --input=<sequence set reference> --mod=<oops|zoops|anr> --nmotifs=<nmotifs> --minw=<minw> --maxw=<maxw> --nsites=<nsites> --minsites=<minsites> --maxsites=<maxsites> --pal --revcomp --user=<username> --pw=<password>]

DESCRIPTION
INPUT:            This command requires the URL of the service, ID of a sequence set and parameters.

OUTPUT:           This command returns Job object ID.

PARAMETERS:
--url             The URL of the service, --url=http://140.221.85.173:7077/, required.

--ws              Workspace name, required.

--input           KBase ID of the input sequence set, required.

--mod             Distribution of motifs (oops =  One Occurrence Per Sequence, zoops = Zero or One Occurrence Per Sequence, anr = Any Number of Repetitions), required.

--nmotifs         Maximum number of motifs to find, required.

--minw            Minumum motif width, required.

--maxw            Maximum motif width, required.

--nsites          Number of sites for each motif.

--minsites        Minimum number of sites for each motif.

--maxsites        Maximum number of sites for each motif.

--pal             Force palindromes.

--revcomp         Allow sites on + or - DNA strands. Default: look for DNA motifs only on the strand given in the training set.

--user            User name for access to workspace.

--pw              Password for access to workspace.

--help            Display help message to standard out and exit with error code zero; 
                  ignore all other command-line arguments.  
--version         Print version information. 

 
EXAMPLES 
find_motifs_with_meme_job_from_ws --url=http://140.221.85.173:7077/ --ws=AKtest --input="AKtest/kb|sequenceset.8" --mod=oops --nmotifs=2 --minw=14 --maxw=28 

This command will return a Job object ID.


Report bugs to aekazakov@lbl.gov
----------TEST meme get_pspm_collection_from_meme_result_from_ws with -h option
	okay

NAME
get_pspm_collection_from_meme_result_from_ws - converts MEME run result into collection of position-specific probability matrices


VERSION
1.0

SYNOPSIS
get_pspm_collection_from_meme_result_from_ws [--url=http://140.221.85.173:7077/ --ws=<workspace name> --input=<MemeRunResult reference> --user=<username> --pw=<password>]

DESCRIPTION
INPUT:            This command requires the URL of the service, workspace name, workspace reference of MemeRunresult, .

OUTPUT:           The output of this command is a name of MemePSPMCollection.

PARAMETERS:
--url             The URL of the service, --url=http://140.221.85.173:7077/, required.

--ws              Workspace name, required.

--input           Workspace reference of the MemeRunResult , required.

--user            User name for access to workspace.

--pw              Password for access to workspace.

--help            Display help message to standard out and exit with error code zero; 
                  ignore all other command-line arguments.  
--version         Print version information. 

 
EXAMPLES 
get_pspm_collection_from_meme_result_from_ws --url=http://140.221.85.173:7077/ --ws=AKtest --input="AKtest/kb|memerunresult.15"

 
Report bugs to aekazakov@lbl.gov
NAME
get_pspm_collection_from_meme_result_from_ws - converts MEME run result into collection of position-specific probability matrices


VERSION
1.0

SYNOPSIS
get_pspm_collection_from_meme_result_from_ws [--url=http://140.221.85.173:7077/ --ws=<workspace name> --input=<MemeRunResult reference> --user=<username> --pw=<password>]

DESCRIPTION
INPUT:            This command requires the URL of the service, workspace name, workspace reference of MemeRunresult, .

OUTPUT:           The output of this command is a name of MemePSPMCollection.

PARAMETERS:
--url             The URL of the service, --url=http://140.221.85.173:7077/, required.

--ws              Workspace name, required.

--input           Workspace reference of the MemeRunResult , required.

--user            User name for access to workspace.

--pw              Password for access to workspace.

--help            Display help message to standard out and exit with error code zero; 
                  ignore all other command-line arguments.  
--version         Print version information. 

 
EXAMPLES 
get_pspm_collection_from_meme_result_from_ws --url=http://140.221.85.173:7077/ --ws=AKtest --input="AKtest/kb|memerunresult.15"

 
Report bugs to aekazakov@lbl.gov
----------TEST meme get_pspm_collection_from_meme_result_from_ws with --help option
	okay

NAME
get_pspm_collection_from_meme_result_from_ws - converts MEME run result into collection of position-specific probability matrices


VERSION
1.0

SYNOPSIS
get_pspm_collection_from_meme_result_from_ws [--url=http://140.221.85.173:7077/ --ws=<workspace name> --input=<MemeRunResult reference> --user=<username> --pw=<password>]

DESCRIPTION
INPUT:            This command requires the URL of the service, workspace name, workspace reference of MemeRunresult, .

OUTPUT:           The output of this command is a name of MemePSPMCollection.

PARAMETERS:
--url             The URL of the service, --url=http://140.221.85.173:7077/, required.

--ws              Workspace name, required.

--input           Workspace reference of the MemeRunResult , required.

--user            User name for access to workspace.

--pw              Password for access to workspace.

--help            Display help message to standard out and exit with error code zero; 
                  ignore all other command-line arguments.  
--version         Print version information. 

 
EXAMPLES 
get_pspm_collection_from_meme_result_from_ws --url=http://140.221.85.173:7077/ --ws=AKtest --input="AKtest/kb|memerunresult.15"

 
Report bugs to aekazakov@lbl.gov
NAME
get_pspm_collection_from_meme_result_from_ws - converts MEME run result into collection of position-specific probability matrices


VERSION
1.0

SYNOPSIS
get_pspm_collection_from_meme_result_from_ws [--url=http://140.221.85.173:7077/ --ws=<workspace name> --input=<MemeRunResult reference> --user=<username> --pw=<password>]

DESCRIPTION
INPUT:            This command requires the URL of the service, workspace name, workspace reference of MemeRunresult, .

OUTPUT:           The output of this command is a name of MemePSPMCollection.

PARAMETERS:
--url             The URL of the service, --url=http://140.221.85.173:7077/, required.

--ws              Workspace name, required.

--input           Workspace reference of the MemeRunResult , required.

--user            User name for access to workspace.

--pw              Password for access to workspace.

--help            Display help message to standard out and exit with error code zero; 
                  ignore all other command-line arguments.  
--version         Print version information. 

 
EXAMPLES 
get_pspm_collection_from_meme_result_from_ws --url=http://140.221.85.173:7077/ --ws=AKtest --input="AKtest/kb|memerunresult.15"

 
Report bugs to aekazakov@lbl.gov
----------TEST meme find_sites_with_mast_by_collection_from_ws with -h option
	okay

NAME
find_sites_with_mast_by_collection_from_ws - search a sequence database for occurences of known motifs by MAST.


VERSION
1.0

SYNOPSIS
find_sites_with_mast_by_collection_from_ws [--url=http://140.221.85.173:7077/ --ws=<workspace ID> --query=<MemePSPMCollection ID> --target=<sequence set ID> --matrix=<PSPM ID> --mt=<mt> --user=<username> --pw=<password>]

DESCRIPTION
INPUT:            This command requires the URL of the service, ID of query PSPM collection, ID of target sequence set, p-value threshold .

OUTPUT:           The output of this command is KBase ID of a list of MAST hits.

PARAMETERS:
--url             The URL of the service, --url=http://140.221.85.173:7077/, required.

--ws              Workspace name, required.

--query           Workspace reference of the query PSPM collection, required.

--target          Workspace reference of the target sequence set, required.

--matrix          KBase ID of a MemePSPM from the query collection that will be used. If omitted, all motifs in the query collection will be used.

--mt              Show motif matches with p-value < <mt>, required.

--user            User name for access to workspace.

--pw              Password for access to workspace.

--help            Display help message to standard out and exit with error code zero; 
                  ignore all other command-line arguments.  
--version         Print version information. 

 
EXAMPLES 
find_sites_with_mast_by_collection_from_ws --url=http://140.221.85.173:7077/ --ws=AKtest --query="AKtest/kb|memepspmcollection.1" --target="AKtest/kb|sequenceset.8" --mt=0.001

This command will return name of a list of occurences of motifs from the collection in the set of sequences with p-value below 0.001.

 
Report bugs to aekazakov@lbl.gov
NAME
find_sites_with_mast_by_collection_from_ws - search a sequence database for occurences of known motifs by MAST.


VERSION
1.0

SYNOPSIS
find_sites_with_mast_by_collection_from_ws [--url=http://140.221.85.173:7077/ --ws=<workspace ID> --query=<MemePSPMCollection ID> --target=<sequence set ID> --matrix=<PSPM ID> --mt=<mt> --user=<username> --pw=<password>]

DESCRIPTION
INPUT:            This command requires the URL of the service, ID of query PSPM collection, ID of target sequence set, p-value threshold .

OUTPUT:           The output of this command is KBase ID of a list of MAST hits.

PARAMETERS:
--url             The URL of the service, --url=http://140.221.85.173:7077/, required.

--ws              Workspace name, required.

--query           Workspace reference of the query PSPM collection, required.

--target          Workspace reference of the target sequence set, required.

--matrix          KBase ID of a MemePSPM from the query collection that will be used. If omitted, all motifs in the query collection will be used.

--mt              Show motif matches with p-value < <mt>, required.

--user            User name for access to workspace.

--pw              Password for access to workspace.

--help            Display help message to standard out and exit with error code zero; 
                  ignore all other command-line arguments.  
--version         Print version information. 

 
EXAMPLES 
find_sites_with_mast_by_collection_from_ws --url=http://140.221.85.173:7077/ --ws=AKtest --query="AKtest/kb|memepspmcollection.1" --target="AKtest/kb|sequenceset.8" --mt=0.001

This command will return name of a list of occurences of motifs from the collection in the set of sequences with p-value below 0.001.

 
Report bugs to aekazakov@lbl.gov
----------TEST meme find_sites_with_mast_by_collection_from_ws with --help option
	okay

NAME
find_sites_with_mast_by_collection_from_ws - search a sequence database for occurences of known motifs by MAST.


VERSION
1.0

SYNOPSIS
find_sites_with_mast_by_collection_from_ws [--url=http://140.221.85.173:7077/ --ws=<workspace ID> --query=<MemePSPMCollection ID> --target=<sequence set ID> --matrix=<PSPM ID> --mt=<mt> --user=<username> --pw=<password>]

DESCRIPTION
INPUT:            This command requires the URL of the service, ID of query PSPM collection, ID of target sequence set, p-value threshold .

OUTPUT:           The output of this command is KBase ID of a list of MAST hits.

PARAMETERS:
--url             The URL of the service, --url=http://140.221.85.173:7077/, required.

--ws              Workspace name, required.

--query           Workspace reference of the query PSPM collection, required.

--target          Workspace reference of the target sequence set, required.

--matrix          KBase ID of a MemePSPM from the query collection that will be used. If omitted, all motifs in the query collection will be used.

--mt              Show motif matches with p-value < <mt>, required.

--user            User name for access to workspace.

--pw              Password for access to workspace.

--help            Display help message to standard out and exit with error code zero; 
                  ignore all other command-line arguments.  
--version         Print version information. 

 
EXAMPLES 
find_sites_with_mast_by_collection_from_ws --url=http://140.221.85.173:7077/ --ws=AKtest --query="AKtest/kb|memepspmcollection.1" --target="AKtest/kb|sequenceset.8" --mt=0.001

This command will return name of a list of occurences of motifs from the collection in the set of sequences with p-value below 0.001.

 
Report bugs to aekazakov@lbl.gov
NAME
find_sites_with_mast_by_collection_from_ws - search a sequence database for occurences of known motifs by MAST.


VERSION
1.0

SYNOPSIS
find_sites_with_mast_by_collection_from_ws [--url=http://140.221.85.173:7077/ --ws=<workspace ID> --query=<MemePSPMCollection ID> --target=<sequence set ID> --matrix=<PSPM ID> --mt=<mt> --user=<username> --pw=<password>]

DESCRIPTION
INPUT:            This command requires the URL of the service, ID of query PSPM collection, ID of target sequence set, p-value threshold .

OUTPUT:           The output of this command is KBase ID of a list of MAST hits.

PARAMETERS:
--url             The URL of the service, --url=http://140.221.85.173:7077/, required.

--ws              Workspace name, required.

--query           Workspace reference of the query PSPM collection, required.

--target          Workspace reference of the target sequence set, required.

--matrix          KBase ID of a MemePSPM from the query collection that will be used. If omitted, all motifs in the query collection will be used.

--mt              Show motif matches with p-value < <mt>, required.

--user            User name for access to workspace.

--pw              Password for access to workspace.

--help            Display help message to standard out and exit with error code zero; 
                  ignore all other command-line arguments.  
--version         Print version information. 

 
EXAMPLES 
find_sites_with_mast_by_collection_from_ws --url=http://140.221.85.173:7077/ --ws=AKtest --query="AKtest/kb|memepspmcollection.1" --target="AKtest/kb|sequenceset.8" --mt=0.001

This command will return name of a list of occurences of motifs from the collection in the set of sequences with p-value below 0.001.

 
Report bugs to aekazakov@lbl.gov
----------TEST meme find_sites_with_mast_job_by_collection_from_ws with -h option
	okay

NAME
find_sites_with_mast_job_by_collection_from_ws - search a sequence database for occurences of known motifs by MAST.


VERSION
1.0

SYNOPSIS
find_sites_with_mast_job_by_collection_from_ws [--url=http://140.221.85.173:7077/ --ws=<workspace ID> --query=<MemePSPMCollection ID> --target=<sequence set ID> --matrix=<PSPM ID> --mt=<mt> --user=<username> --pw=<password>]

DESCRIPTION
INPUT:            This command requires the URL of the service, ID of query PSPM collection, ID of target sequence set, p-value threshold .

OUTPUT:           The output of this command is Job object ID that keeps ID of a list of MAST hits.

PARAMETERS:
--url             The URL of the service, --url=http://140.221.85.173:7077/, required.

--ws              Workspace name, required.

--query           Workspace reference of the query PSPM collection, required.

--target          Workspace reference of the target sequence set, required.

--matrix          KBase ID of a MemePSPM from the query collection that will be used. If omitted, all motifs in the query collection will be used.

--mt              Show motif matches with p-value < <mt>, required.

--user            User name for access to workspace.

--pw              Password for access to workspace.

--help            Display help message to standard out and exit with error code zero; 
                  ignore all other command-line arguments.  
--version         Print version information. 

 
EXAMPLES 
find_sites_with_mast_job_by_collection_from_ws --url=http://140.221.85.173:7077/ --ws=AKtest --query="AKtest/kb|memepspmcollection.2" --target="AKtest/kb|sequenceset.8" --mt=0.001

This command will return a Job object ID.

 
Report bugs to aekazakov@lbl.gov
NAME
find_sites_with_mast_job_by_collection_from_ws - search a sequence database for occurences of known motifs by MAST.


VERSION
1.0

SYNOPSIS
find_sites_with_mast_job_by_collection_from_ws [--url=http://140.221.85.173:7077/ --ws=<workspace ID> --query=<MemePSPMCollection ID> --target=<sequence set ID> --matrix=<PSPM ID> --mt=<mt> --user=<username> --pw=<password>]

DESCRIPTION
INPUT:            This command requires the URL of the service, ID of query PSPM collection, ID of target sequence set, p-value threshold .

OUTPUT:           The output of this command is Job object ID that keeps ID of a list of MAST hits.

PARAMETERS:
--url             The URL of the service, --url=http://140.221.85.173:7077/, required.

--ws              Workspace name, required.

--query           Workspace reference of the query PSPM collection, required.

--target          Workspace reference of the target sequence set, required.

--matrix          KBase ID of a MemePSPM from the query collection that will be used. If omitted, all motifs in the query collection will be used.

--mt              Show motif matches with p-value < <mt>, required.

--user            User name for access to workspace.

--pw              Password for access to workspace.

--help            Display help message to standard out and exit with error code zero; 
                  ignore all other command-line arguments.  
--version         Print version information. 

 
EXAMPLES 
find_sites_with_mast_job_by_collection_from_ws --url=http://140.221.85.173:7077/ --ws=AKtest --query="AKtest/kb|memepspmcollection.2" --target="AKtest/kb|sequenceset.8" --mt=0.001

This command will return a Job object ID.

 
Report bugs to aekazakov@lbl.gov
----------TEST meme find_sites_with_mast_job_by_collection_from_ws with --help option
	okay

NAME
find_sites_with_mast_job_by_collection_from_ws - search a sequence database for occurences of known motifs by MAST.


VERSION
1.0

SYNOPSIS
find_sites_with_mast_job_by_collection_from_ws [--url=http://140.221.85.173:7077/ --ws=<workspace ID> --query=<MemePSPMCollection ID> --target=<sequence set ID> --matrix=<PSPM ID> --mt=<mt> --user=<username> --pw=<password>]

DESCRIPTION
INPUT:            This command requires the URL of the service, ID of query PSPM collection, ID of target sequence set, p-value threshold .

OUTPUT:           The output of this command is Job object ID that keeps ID of a list of MAST hits.

PARAMETERS:
--url             The URL of the service, --url=http://140.221.85.173:7077/, required.

--ws              Workspace name, required.

--query           Workspace reference of the query PSPM collection, required.

--target          Workspace reference of the target sequence set, required.

--matrix          KBase ID of a MemePSPM from the query collection that will be used. If omitted, all motifs in the query collection will be used.

--mt              Show motif matches with p-value < <mt>, required.

--user            User name for access to workspace.

--pw              Password for access to workspace.

--help            Display help message to standard out and exit with error code zero; 
                  ignore all other command-line arguments.  
--version         Print version information. 

 
EXAMPLES 
find_sites_with_mast_job_by_collection_from_ws --url=http://140.221.85.173:7077/ --ws=AKtest --query="AKtest/kb|memepspmcollection.2" --target="AKtest/kb|sequenceset.8" --mt=0.001

This command will return a Job object ID.

 
Report bugs to aekazakov@lbl.gov
NAME
find_sites_with_mast_job_by_collection_from_ws - search a sequence database for occurences of known motifs by MAST.


VERSION
1.0

SYNOPSIS
find_sites_with_mast_job_by_collection_from_ws [--url=http://140.221.85.173:7077/ --ws=<workspace ID> --query=<MemePSPMCollection ID> --target=<sequence set ID> --matrix=<PSPM ID> --mt=<mt> --user=<username> --pw=<password>]

DESCRIPTION
INPUT:            This command requires the URL of the service, ID of query PSPM collection, ID of target sequence set, p-value threshold .

OUTPUT:           The output of this command is Job object ID that keeps ID of a list of MAST hits.

PARAMETERS:
--url             The URL of the service, --url=http://140.221.85.173:7077/, required.

--ws              Workspace name, required.

--query           Workspace reference of the query PSPM collection, required.

--target          Workspace reference of the target sequence set, required.

--matrix          KBase ID of a MemePSPM from the query collection that will be used. If omitted, all motifs in the query collection will be used.

--mt              Show motif matches with p-value < <mt>, required.

--user            User name for access to workspace.

--pw              Password for access to workspace.

--help            Display help message to standard out and exit with error code zero; 
                  ignore all other command-line arguments.  
--version         Print version information. 

 
EXAMPLES 
find_sites_with_mast_job_by_collection_from_ws --url=http://140.221.85.173:7077/ --ws=AKtest --query="AKtest/kb|memepspmcollection.2" --target="AKtest/kb|sequenceset.8" --mt=0.001

This command will return a Job object ID.

 
Report bugs to aekazakov@lbl.gov


Error Summary:

